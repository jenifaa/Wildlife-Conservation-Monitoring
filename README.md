## 1. PostgreSQL কী?

PostgreSQL হলো একটি বিনামূল্যের ও ওপেন সোর্স ডেটাবেস ম্যানেজমেন্ট সিস্টেম। এটি মূলত এমন একটি সফটওয়্যার যা ডেটা সংরক্ষণ, সাজানো এবং প্রয়োজনে খুঁজে পেতে সাহায্য করে। যখন বিশাল পরিমাণ বিস্তারিত তথ্য গুছিয়ে ও নিরাপদভাবে সংরক্ষণের প্রয়োজন হয়, তখন PostgreSQL ব্যবহার করাটা সবচেয়ে উপযুক্ত।

#### PostgreSQL যা করতে পারে:

✅ তথ্য সংরক্ষণ করতে,

✅ বড় বড় তথ্য সেট ম্যানেজ করতে,

✅ খুব দ্রুত তথ্য খুঁজে বের করতে,

✅ একাধিক টেবিলের মধ্যে সম্পর্ক (Relationship) তৈরি করতে 

### PostgreSQL-এর গুরুত্বপূর্ণ ফিচার:

✅ ওপেন সোর্স: একেবারে ফ্রি ও কাস্টমাইজ করা যায়,

📚 রিলেশনাল ডেটাবেস: তথ্য গুছিয়ে টেবিলে রাখা যায় (যেমন: Excel),

🧠 SQL ব্যবহার করে প্রশ্ন করা যায়	যেমন: কে সবচেয়ে বেশি নাম্বার পেয়েছে?,

🔗 সম্পর্ক তৈরি করা যায়	যেমন: ছাত্র ও তার মার্কসের মধ্যে সম্পর্ক,

📦 JSON সাপোর্ট করে	জটিল ডেটা টাইপও রাখা যায়,

🔒 নিরাপদ ইউজার পারমিশনসহ ডেটা সুরক্ষা,

📈 স্কেলযোগ্য ছোট অ্যাপ থেকে শুরু করে বড় ব্যাংকিং সিস্টেমেও চলে

### PostgreSQL কোথায় কোথায় ব্যবহার হয়?

#### PostgreSQL ব্যবহৃত হয়:

✅ ওয়েব অ্যাপ্লিকেশন (যেমন: Twitter, Reddit)

✅ ব্যাংকিং সিস্টেম

✅ ই-কমার্স ওয়েবসাইট

✅ সরকারি অফিস ও হাসপাতালের তথ্য সংরক্ষণে

✅ ডেটা বিশ্লেষণ বা রিপোর্টিং-এ

### 🏪 উদাহরণ: একটি অনলাইন দোকানের প্রোডাক্ট ডেটা ম্যানেজমেন্ট

ধরো, তোমার একটা ইলেকট্রনিক্স দোকান আছে যেখানে হাজার হাজার পণ্য (products) আছে। এখন তুমি PostgreSQL ব্যবহার করে সেই ডেটা সংরক্ষণ করবে ও CRUD অপারেশন করবে।

#### ✅ ১. 📦 products টেবিল তৈরি করো:
```
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  description TEXT,
  price NUMERIC(10, 2),
  quantity INT,
  category VARCHAR(50)
);
```
#### 📝 এখানে:

name = পণ্যের নাম

description = পণ্যের বিস্তারিত

price = মূল্য

quantity = স্টকে কত আছে

category = কোন ক্যাটাগরির পণ্য (যেমন Mobile, Laptop)

#### ✅ ২. ➕ নতুন পণ্য যোগ করো (Create):
```
INSERT INTO products (name, description, price, quantity, category)
VALUES ('Samsung Galaxy S23', 'Latest model with AMOLED display', 899.99, 20, 'Mobile');
```

#### ✅ ৩. 📖 সব পণ্য দেখো (Read):
```
SELECT * FROM products;
```

#### ✅ ৪. ✏️ পণ্যের দাম ও স্টক আপডেট করো (Update):
```
UPDATE products
SET price = 849.99, quantity = 15
WHERE id = 1;
```
#### ✅ ৫. ❌ একটি পণ্য ডিলিট করো (Delete):
```
DELETE FROM products
WHERE id = 1;
```


## 2. Primary Key (প্রাইমারি কি) কী? এবং Foreign Key (ফরেন কি) কী?

 ### Primary Key:

Primary Key হচ্ছে এমন  কলাম  যার মান প্রতিটি রেকর্ডের (row) জন্য একটি ইউনিক আইডেন্টিফায়ার হয়।

✅ এটি অবশ্যই ইউনিক (দু'বার একই মান রাখা যাবে না)

✅ এটি null হতে পারে না

✅ প্রতিটি টেবিলের মধ্যে শুধু একটি Primary Key থাকতে পারে

#### কি কাজ করে?

✅ Primary Key হচ্ছে টেবিলের প্রতিটি রেকর্ডকে সুনির্দিষ্টভাবে শনাক্ত করার জন্য।

✅ এটি এমন একটি ফিল্ড (বা একাধিক ফিল্ড), যার মাধ্যমে প্রতিটি রো (row) আলাদা করে চেনা যায়।

#### উদাহরণ (Primary Key):
```
CREATE TABLE products (
  product_id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  price NUMERIC(10, 2)
);
```
product_id এখানে Primary Key।

প্রতিটি পণ্যের জন্য এটি ইউনিক হবে: 1, 2, 3...

### Foreign Key: 

 Foreign Key এমন একটি কলাম যা অন্য একটি টেবিলের Primary Key এর সাথে সম্পর্কযুক্ত (Link) থাকে।

✅ এক টেবিলের রেকর্ডের সাথে অন্য টেবিলের রেকর্ডকে সংযুক্ত করে

✅ এটি টেবিলগুলোর মধ্যে সম্পর্ক (Relationship) তৈরি করে

✅ এটি ডেটার ইন্টিগ্রিটি বজায় রাখে (ভুল ডেটা ঢুকতে দেয় না)


#### ✅ কাজের ধরন:
তুমি যদি চাও orders টেবিল যেন customers টেবিলের সাথে যুক্ত থাকে, তবে orders.customer_id হবে customers.customer_id এর foreign key।

#### ✅ বৈশিষ্ট্য (Features):

1. Parent-Child সম্পর্ক তৈরি করে
→ customers = parent, orders = child

2. Referential Integrity বজায় রাখে
→ যদি parent-এ কোন id না থাকে, child-এ সেই id ঢুকবে না।

3. ON DELETE / ON UPDATE rules
→ তুমি নির্ধারণ করতে পারো, parent রেকর্ড ডিলিট হলে child-এ কি হবে।


#### উদাহরণ (Foreign Key):
```
CREATE TABLE customers (
  customer_id SERIAL PRIMARY KEY,
  name VARCHAR(100)
);

CREATE TABLE orders (
  order_id SERIAL PRIMARY KEY,
  order_date DATE,
  customer_id INT,
  FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
);
```
orders টেবিলের customer_id কলামটি হচ্ছে Foreign Key।


## 3. VARCHAR এবং CHAR এর মধ্যে পার্থক্য কী ?

### ✅ CHAR (Character):

CHAR হচ্ছে একটি Fixed-length (নির্দিষ্ট দৈর্ঘ্যের) ডেটা টাইপ।

মান যেটাই হোক, যতটুকু জায়গা বরাদ্দ করা হয়েছে, পুরোটা জুড়ে রাখা হবে। যদি কম ক্যারেক্টার হয়, তাহলে বাকি জায়গা space (ফাঁকা) দিয়ে পূরণ করা হবে।

### ✅ VARCHAR (Variable Character):

VARCHAR হচ্ছে একটি Variable-length (পরিবর্তনযোগ্য দৈর্ঘ্যের) ডেটা টাইপ।

এটি কেবলমাত্র সেই পরিমাণ জায়গা ব্যবহার করে, যতটুকু ক্যারেক্টার রাখা হয়েছে।

#### 📊 তুলনামূলক পার্থক্য:

| বিষয়              | CHAR(n)                                                 | VARCHAR(n)                                            |
|-------------------|---------------------------------------------------------|-------------------------------------------------------|
|                   |                                                         |                                                       |
| দৈর্ঘ্য              | Fixed (স্থির) দৈর্ঘ্য                                         | Variable (পরিবর্তনশীল) দৈর্ঘ্য                            |
|                   |                                                         |                                                       |
| Storage           | সবসময় নির্দিষ্ট (n) বাইট ব্যবহার করে                         | যতটুকু প্রয়োজন, ততটুকু বাইট ব্যবহার করে                 |
|                   |                                                          |                                                      |
| পারফরম্যান্স        | সামান্য দ্রুত হতে পারে (কারণ দৈর্ঘ্য নির্দিষ্ট)                     | বড় ভলিউমে একটু ধীর হতে পারে                         |
|                   |                                                          |                                                      |
| Trailing Space    | স্বয়ংক্রিয়ভাবে ফাঁকা জায়গা যোগ হয়                           | স্বয়ংক্রিয়ভাবে ফাঁকা জায়গা যোগ হয় না                    |
|                   |                                                          |                                                      |
| ব্যবহারের ক্ষেত্র      | কোড, পোস্টাল কোড, জেন্ডার – যেখানে দৈর্ঘ্য সবসময় এক      | নাম, ইমেইল, ঠিকানা – যেখানে দৈর্ঘ্য ভিন্ন হতে পারে         |



#### উদাহরণ:

#### CHAR(10):

```
CREATE TABLE users (
  code CHAR(10)
);
```
```
INSERT INTO users (code) VALUES ('ABCD');
```

এখানে ABCD এর দৈর্ঘ্য 4।
কিন্তু CHAR(10) ব্যবহার করার কারণে এটা "ABCD      " (৬টি স্পেস সহ) হয়ে যাবে।

#### VARCHAR(10):
```
CREATE TABLE users (
  code VARCHAR(10)
);
```
```
INSERT INTO users (code) VALUES ('ABCD');
```
এখানে ABCD শুধুই 4 ক্যারেক্টার হিসেবেই সংরক্ষিত হবে, বাড়তি স্পেস থাকবে না।

#### বাস্তব উদাহরণ ও প্রয়োগ:
✉️ Email ফিল্ড:
```
email VARCHAR(100)
```
কারণ: ইমেইলের দৈর্ঘ্য ভিন্ন হতে পারে।

🏷️ Gender ফিল্ড:
```
gender CHAR(1)
```
কারণ: এখানে শুধুই 'M' বা 'F' হবে — সবসময় ১ ক্যারেক্টার।

🔢 PIN Code:
```
pin_code CHAR(6)
```
সব সময় ৬ ডিজিট হওয়ায় এটা CHAR হলে ভালো।

#### কখন কোনটা ব্যবহার করবো?

মানের দৈর্ঘ্য সবসময় সমান - CHAR

মানের দৈর্ঘ্য ভিন্ন ভিন্ন - VARCHAR

পারফরম্যান্স গুরুত্বপূর্ণ ও স্পেস ফিক্সড - CHAR

স্পেস বাঁচাতে চাও এবং ডেটা বড় -  VARCHAR


## 4. SELECT স্টেটমেন্টে WHERE ক্লজটি কী কাজ করে তা ব্যাখ্যা করুন।

### SELECT স্টেটমেন্টে:

SELECT স্টেটমেন্ট হল SQL-এর একটি কমান্ড, যার মাধ্যমে ডেটাবেজের একটি বা একাধিক টেবিল থেকে তথ্য (ডেটা) বের করে আনা হয়। এটি ব্যবহার করে নির্দিষ্ট কলাম বা সব কলামের তথ্য দেখা যায়।

রূপ (Syntax):
```
SELECT কলাম১, কলাম২, ...
FROM টেবিলের_নাম;
```

#### উদাহরণ:
```
SELECT name, age FROM students;
```

এর মানে: students টেবিল থেকে name ও age কলামের ডেটা দেখাও।



### WHERE ক্লজ কী?

SQL-এ WHERE ক্লজ ব্যবহার করা হয় একটি নির্দিষ্ট শর্ত অনুযায়ী ডেটা ফিল্টার করার জন্য। অর্থাৎ, আপনি যদি চান শুধু নির্দিষ্ট কিছু ডেটা দেখতে যেটা কোনও শর্ত পূরণ করে, তখন WHERE ক্লজ ব্যবহার করা হয়।

#### উদাহরণ দিয়ে ব্যাখ্যা:

ধরুন আমাদের একটি টেবিল আছে যার নাম `students`:



| id  | name   | age | grade |
|-----|--------|-----|-------|
| 1   | Nahida | 18  | A     |
| 2   | Jenifa | 20  | B     |
| 3   | Urmi   | 19  | A     |
| 4   | Mili   | 21  | C     |

#### SELECT স্টেটমেন্ট কী করে?
```
SELECT * FROM students;
```

এটি students টেবিলের সকল রেকর্ড দেখাবে। এতে কোনও শর্ত নেই।

#### WHERE ক্লজ যুক্ত করলে কী হয়?
```
SELECT * FROM students WHERE grade = 'A';
```

 এখানে WHERE grade = 'A' শর্তটি বলে দিচ্ছে:

শুধুমাত্র সেই সব রেকর্ড দেখাও যেখানে grade এর মান 'A'।

🟢 ফলাফল হবে:

| id  | name  | age | grade |
|-----|-------|-----|-------|
| 1   | Nahid | 18  | A     |
| 3   | Tamim | 19  | A     |


## 5. JOIN অপারেশন কী এবং এর গুরুত্ব?

SQL-এর JOIN অপারেশনটি একাধিক টেবিলের মধ্যে সম্পর্ক স্থাপন করে এবং তাদের ডেটা একত্রে (combine) দেখার সুযোগ দেয়। এটি তখনই দরকার হয় যখন আমাদের ডেটা বিভিন্ন টেবিলে ভাগ করে রাখা হয়েছে (যেমন একটি নর্মালাইজড ডেটাবেজে হয়ে থাকে) এবং আমরা সেই সব টেবিল থেকে একসাথে তথ্য আনতে চাই।

##### JOIN মূলত "এক টেবিলের ডেটা অন্য টেবিলের সাথে মিলিয়ে দেখানো"।



#### উদাহরণ: একটি দোকানের (Shop) ডেটাবেজ

ধরুন আমাদের একটি Shop Management System আছে, যার মধ্যে দুটি টেবিল রয়েছে:

#### Table 1: `products`

| product_id | product_name | price |
|------------|--------------|-------|
| 1          | Laptop       | 800   |
| 2          | Mouse        | 20    |
| 3          | Keyboard     | 40    |

#### Table 2: `orders`

| order_id | customer_name | product_id | quantity |
|----------|---------------|------------|----------|
| 101      | Nahid         | 1          | 2        |
| 102      | Jenifa        | 2          | 3        |
| 103      | Urmi          | 3          | 1        |




আমরা এমন একটি রিপোর্ট তৈরি করতে চাই, যেখানে দেখাবে:

1. কোন কাস্টমার কোন প্রোডাক্ট কিনেছে

2. সেই প্রোডাক্টের নাম ও দাম

3. কত কন পরিমাণে কিনেছে

4. মোট মূল্য (quantity × price)

#### আমরা orders এবং products টেবিলকে product_id ফিল্ড দিয়ে JOIN করব।
```
SELECT 
  orders.order_id,
  orders.customer_name,
  products.product_name,
  products.price,
  orders.quantity,
  (products.price * orders.quantity) AS total_price
FROM orders
JOIN products
ON orders.product_id = products.product_id;

```
#### Output: 

| order_id | customer_name | product_name | price | quantity | total_price |
|----------|---------------|--------------|-------|----------|-------------|
| 101      | Nahid         | Laptop       | 800   | 2        | 1600        |
| 102      | Jenifa        | Mouse        | 20    | 3        | 60          |
| 103      | Urmi          | Keyboard     | 40    | 1        | 40          |


#### JOIN কিভাবে কাজ করে?

JOIN কমান্ডটি orders.product_id এবং products.product_id এই দুই টেবিলের মিল খোঁজে।

যেখানে এই দুই ফিল্ডের মান একই, সেখানকার রো (row) গুলোকে একত্রিত করে।

তারপর আমরা SELECT দিয়ে যেসব কলাম চাই সেগুলো বের করি।

| JOIN টাইপ       | ব্যাখ্যা                                             |
|-----------------|-------------------------------------------------- |
| INNER JOIN      | শুধু মিল পাওয়া রেকর্ড গুলো দেখায়                    |
| LEFT JOIN       | বাম টেবিলের সব রেকর্ড + মিল পাওয়া ডান টেবিলের ডেটা |
| RIGHT JOIN      | ডান টেবিলের সব রেকর্ড + মিল পাওয়া বাম টেবিলের ডেটা |
| FULL OUTER JOIN | দুই টেবিলের সব রেকর্ড দেখায়, মিল থাকুক বা না থাকুক  |


#### LEFT JOIN উদাহরণ:

ধরুন কেউ অর্ডার করেনি, তবুও আমরা `orders` টেবিল থেকে সব কিছু দেখতে চাই, তাহলে:

```
SELECT 
  orders.order_id,
  orders.customer_name,
  products.product_name
FROM orders
LEFT JOIN products
ON orders.product_id = products.product_id;
```

এটি অর্ডার টেবিলের সব রেকর্ড দেখাবে, এমনকি যদি কোন প্রোডাক্টের সাথে মিল না থাকে।

#### কেন JOIN গুরুত্বপূর্ণ?


1. রিলেশনাল ডেটাবেজে টেবিলগুলো আলাদা থাকে — একত্রিত করতে JOIN দরকার।

2. রিপোর্ট তৈরি করা সহজ হয় — যেমন, অর্ডার রিপোর্ট, ইনভয়েস তৈরি ইত্যাদি।

3. ডেটার পুনরাবৃত্তি (redundancy) কমে — সব ডেটা এক টেবিলে না রেখে রিলেশন করে JOIN দিয়ে আনা যায়।

4. পারফরম্যান্স ও স্ট্রাকচার বজায় থাকে — ডেটা সুগঠিত থাকে।